import React from 'react';
import { Route, Routes } from 'react-router-dom';

const NewStudyNavigation = () => {
    return (
        <Routes>
        {/* <Route
            path={NavigationEnum.preview}
            element={<PreviewPage />}
        /> */}
        </Routes>
    );
};

export default NewStudyNavigation;